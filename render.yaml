# Render Blueprint pour déploiement automatique sur render.com
services:
  - type: web
    name: korean-master
    env: static
    buildCommand: npm install --include=dev && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
